// Screenshot-harness stub: views call send() on user actions; on mount it just
// resolves a benign value so the view renders its chrome with no service worker.
export async function send(): Promise<never> { return undefined as never; }
